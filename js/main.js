"use strict";

import data from './data.js';
import mustache from './mustache.js';


(function(){
    console.log('done loading!');
    
    let password = 'superslaptas';

    function suma(a, b) {
        const sum = a+b;
        return sum;
    }

    console.log(data);
    
})();
